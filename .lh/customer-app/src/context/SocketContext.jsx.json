{
    "sourceFile": "customer-app/src/context/SocketContext.jsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 4,
            "patches": [
                {
                    "date": 1776010554042,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1776010722990,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,25 +1,32 @@\n-import { createContext, useContext, useEffect, useState } from 'react';\r\n+import { createContext, useContext, useEffect, useState, useRef } from 'react';\r\n import { useAuth } from './AuthContext';\r\n import { createSocket } from '../services/socket';\r\n \r\n const SocketContext = createContext();\r\n \r\n export const SocketProvider = ({ children }) => {\r\n   const { user } = useAuth();\r\n   const [socket, setSocket] = useState(null);\r\n+  const socketRef = useRef(null);\r\n \r\n   useEffect(() => {\r\n     if (user) {\r\n       const token = localStorage.getItem('token');\r\n       const newSocket = createSocket(token);\r\n+      socketRef.current = newSocket;\r\n       setSocket(newSocket);\r\n-      return () => newSocket.close();\r\n+\r\n+      return () => {\r\n+        newSocket.close();\r\n+        socketRef.current = null;\r\n+      };\r\n     } else {\r\n-      if (socket) {\r\n-        socket.close();\r\n-        setSocket(null);\r\n+      if (socketRef.current) {\r\n+        socketRef.current.close();\r\n+        socketRef.current = null;\r\n       }\r\n+      setSocket(null);\r\n     }\r\n   }, [user]);\r\n \r\n   return (\r\n@@ -28,5 +35,6 @@\n     </SocketContext.Provider>\r\n   );\r\n };\r\n \r\n+// eslint-disable-next-line react-refresh/only-export-components\r\n export const useSocket = () => useContext(SocketContext);\n\\ No newline at end of file\n"
                },
                {
                    "date": 1776010857522,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,28 +6,35 @@\n \r\n export const SocketProvider = ({ children }) => {\r\n   const { user } = useAuth();\r\n   const [socket, setSocket] = useState(null);\r\n-  const socketRef = useRef(null);\r\n+  const userRef = useRef(user);\r\n \r\n   useEffect(() => {\r\n-    if (user) {\r\n-      const token = localStorage.getItem('token');\r\n-      const newSocket = createSocket(token);\r\n-      socketRef.current = newSocket;\r\n-      setSocket(newSocket);\r\n+    userRef.current = user;\r\n+  }, [user]);\r\n \r\n-      return () => {\r\n-        newSocket.close();\r\n-        socketRef.current = null;\r\n-      };\r\n-    } else {\r\n-      if (socketRef.current) {\r\n-        socketRef.current.close();\r\n-        socketRef.current = null;\r\n-      }\r\n+  useEffect(() => {\r\n+    if (!user) {\r\n       setSocket(null);\r\n+      return;\r\n     }\r\n+\r\n+    let isMounted = true;\r\n+    const token = localStorage.getItem('token');\r\n+    const newSocket = createSocket(token);\r\n+\r\n+    // Use a microtask or timeout to avoid synchronous setState warning\r\n+    Promise.resolve().then(() => {\r\n+      if (isMounted) {\r\n+        setSocket(newSocket);\r\n+      }\r\n+    });\r\n+\r\n+    return () => {\r\n+      isMounted = false;\r\n+      newSocket.close();\r\n+    };\r\n   }, [user]);\r\n \r\n   return (\r\n     <SocketContext.Provider value={socket}>\r\n"
                },
                {
                    "date": 1776010876047,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,47 +1,13 @@\n-import { createContext, useContext, useEffect, useState, useRef } from 'react';\r\n-import { useAuth } from './AuthContext';\r\n-import { createSocket } from '../services/socket';\r\n+useEffect(() => {\r\n+  if (!user) {\r\n+    setSocket(null);\r\n+    return;\r\n+  }\r\n \r\n-const SocketContext = createContext();\r\n+  const token = localStorage.getItem('token');\r\n+  const newSocket = createSocket(token);\r\n+  // eslint-disable-next-line react-hooks/set-state-in-effect\r\n+  setSocket(newSocket);\r\n \r\n-export const SocketProvider = ({ children }) => {\r\n-  const { user } = useAuth();\r\n-  const [socket, setSocket] = useState(null);\r\n-  const userRef = useRef(user);\r\n-\r\n-  useEffect(() => {\r\n-    userRef.current = user;\r\n-  }, [user]);\r\n-\r\n-  useEffect(() => {\r\n-    if (!user) {\r\n-      setSocket(null);\r\n-      return;\r\n-    }\r\n-\r\n-    let isMounted = true;\r\n-    const token = localStorage.getItem('token');\r\n-    const newSocket = createSocket(token);\r\n-\r\n-    // Use a microtask or timeout to avoid synchronous setState warning\r\n-    Promise.resolve().then(() => {\r\n-      if (isMounted) {\r\n-        setSocket(newSocket);\r\n-      }\r\n-    });\r\n-\r\n-    return () => {\r\n-      isMounted = false;\r\n-      newSocket.close();\r\n-    };\r\n-  }, [user]);\r\n-\r\n\\ No newline at end of file\n-  return (\r\n-    <SocketContext.Provider value={socket}>\r\n-      {children}\r\n-    </SocketContext.Provider>\r\n-  );\r\n-};\r\n-\r\n-// eslint-disable-next-line react-refresh/only-export-components\r\n-export const useSocket = () => useContext(SocketContext);\n+  return () => newSocket.close();\r\n+}, [user]);\n\\ No newline at end of file\n"
                },
                {
                    "date": 1776011467611,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,13 +1,41 @@\n-useEffect(() => {\r\n-  if (!user) {\r\n-    setSocket(null);\r\n-    return;\r\n-  }\r\n+import { createContext, useContext, useEffect, useState, useRef } from 'react';\r\n+import { useAuth } from './AuthContext';\r\n+import { createSocket } from '../services/socket';\r\n \r\n-  const token = localStorage.getItem('token');\r\n-  const newSocket = createSocket(token);\r\n-  // eslint-disable-next-line react-hooks/set-state-in-effect\r\n-  setSocket(newSocket);\r\n\\ No newline at end of file\n+const SocketContext = createContext();\r\n \r\n-  return () => newSocket.close();\r\n-}, [user]);\n+export const SocketProvider = ({ children }) => {\r\n+  const { user } = useAuth();\r\n+  const [socket, setSocket] = useState(null);\r\n+  const socketRef = useRef(null);\r\n+\r\n+  useEffect(() => {\r\n+    if (user) {\r\n+      const token = localStorage.getItem('token');\r\n+      const newSocket = createSocket(token);\r\n+      socketRef.current = newSocket;\r\n+      // eslint-disable-next-line react-hooks/set-state-in-effect\r\n+      setSocket(newSocket);\r\n+\r\n+      return () => {\r\n+        newSocket.close();\r\n+        socketRef.current = null;\r\n+      };\r\n+    } else {\r\n+      if (socketRef.current) {\r\n+        socketRef.current.close();\r\n+        socketRef.current = null;\r\n+      }\r\n+      setSocket(null);\r\n+    }\r\n+  }, [user]);\r\n+\r\n+  return (\r\n+    <SocketContext.Provider value={socket}>\r\n+      {children}\r\n+    </SocketContext.Provider>\r\n+  );\r\n+};\r\n+\r\n+// eslint-disable-next-line react-refresh/only-export-components\r\n+export const useSocket = () => useContext(SocketContext);\n\\ No newline at end of file\n"
                }
            ],
            "date": 1776010554042,
            "name": "Commit-0",
            "content": "import { createContext, useContext, useEffect, useState } from 'react';\r\nimport { useAuth } from './AuthContext';\r\nimport { createSocket } from '../services/socket';\r\n\r\nconst SocketContext = createContext();\r\n\r\nexport const SocketProvider = ({ children }) => {\r\n  const { user } = useAuth();\r\n  const [socket, setSocket] = useState(null);\r\n\r\n  useEffect(() => {\r\n    if (user) {\r\n      const token = localStorage.getItem('token');\r\n      const newSocket = createSocket(token);\r\n      setSocket(newSocket);\r\n      return () => newSocket.close();\r\n    } else {\r\n      if (socket) {\r\n        socket.close();\r\n        setSocket(null);\r\n      }\r\n    }\r\n  }, [user]);\r\n\r\n  return (\r\n    <SocketContext.Provider value={socket}>\r\n      {children}\r\n    </SocketContext.Provider>\r\n  );\r\n};\r\n\r\nexport const useSocket = () => useContext(SocketContext);"
        }
    ]
}